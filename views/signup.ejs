<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="style.css" />
    <title>WPR Email System</title>
  </head>
  <body>
    <div class="container">
      <% if (typeof name !== 'undefined') { %>
      <h1>Welcome, <%= name %>!</h1>
      <p>Your account has been created successfully!.</p>
      <p>You can now <a href="/">login</a>.</p>
      <% } else { %>
      <h1>Sign Up</h1>
      <form method="post" action="/signup" class="signin-form">
        <label for="name" class="signin-label">Full Name:</label>
        <input
          type="text"
          id="name"
          name="name"
          required
          class="input-field"
          placeholder="Enter your full name"
        />
        <br />
        <label for="email" class="signin-label">Email:</label>
        <input
          type="email"
          id="email"
          name="email"
          required
          class="input-field"
          placeholder="Enter your email address"
        />
        <br />
        <label for="password" class="signin-label">Password:</label>
        <input
          type="password"
          id="password"
          name="password"
          required
          class="input-field"
          placeholder="Enter your password"
        />
        <br />
        <label for="confirmPassword" class="signin-label"
          >Re-enter Password:</label
        >
        <input
          type="password"
          id="confirmPassword"
          name="confirmPassword"
          required
          class="input-field"
          placeholder="Re-enter your password"
        />
        <% if (typeof error !== 'undefined') { %>
        <p class="error"><%= error %></p>
        <% } %>
        <button type="submit" class="btn">Sign Up</button>
      </form>

      <p class="signup-link">
        <a href="/">Already have an account?</a>
      </p>
      <% } %>
    </div>
  </body>
</html>
